export const getProjectsList = state => state.projectsList;

export const getProjectNameById = (state, id) => {
  return 'ENABLE CORS!';
  // TODO enable this once CORS is enabled in projects API
  // return state.projectsList[id].short_name;
};
